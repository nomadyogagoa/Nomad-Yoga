import {AdminShell} from "@/components/admin/AdminShell"; import {Icon} from "@/components/ui/Icon"; import {programs} from "@/data/programs";
export default function ProgramsAdmin(){return <AdminShell active="Programs" title="Programs & classes" subtitle="Control programs, schedules, capacity and instructor assignment." action={<button className="button button-small"><Icon name="plus"/> New program</button>}><div className="admin-program-grid">{programs.map((p:any)=><article className="admin-program" key={p.slug}><div className="admin-program-img" style={{backgroundImage:`url(${p.image})`}}/><div><div className="admin-program-top"><span className="soft-chip">Published</span><button className="icon-button"><Icon name="edit"/></button></div><h2>{p.title}</h2><p>{p.shortDescription||p.description}</p><div className="admin-program-meta"><span>{p.duration}</span><span>{p.level}</span></div></div></article>)}</div></AdminShell>}
