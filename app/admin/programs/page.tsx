import {AdminShell} from "@/components/admin/AdminShell"; import {Icon} from "@/components/ui/Icon"; import {Reveal} from "@/components/ui/Reveal"; import {programs} from "@/data/programs"; import Image from "next/image";
export default function ProgramsAdmin(){return <AdminShell active="Programs" title="Programs & classes" subtitle="Control programs, schedules, capacity and instructor assignment." action={<button className="button button-small"><Icon name="plus"/> New program</button>}><div className="admin-program-grid">{programs.map((p:any,i:number)=><Reveal as="article" className="admin-program" key={p.slug} delay={i*0.08}><div className="admin-program-img"><Image src={p.image} alt="" fill sizes="180px" /></div><div><div className="admin-program-top"><span className="soft-chip">Published</span><button className="icon-button"><Icon name="edit"/></button></div><h2>{p.title}</h2><p>{p.shortDescription||p.description}</p><div className="admin-program-meta"><span>{p.duration}</span><span>{p.level}</span></div></div></Reveal>)}</div></AdminShell>}
