import {AdminShell} from "@/components/admin/AdminShell"; import {gallery} from "@/data/extended"; import {Icon} from "@/components/ui/Icon"; import {Reveal} from "@/components/ui/Reveal"; import Image from "next/image";
export default function GalleryAdmin(){return <AdminShell active="Gallery" title="Media library" subtitle="Upload and organize images used across the website." action={<button className="button button-small"><Icon name="plus"/> Upload media</button>}><div className="media-grid">{gallery.map(([t,img],i)=><Reveal as="article" key={t} delay={(i%9)*0.05}><Image src={img} alt="" fill sizes="(max-width: 1100px) 50vw, 33vw" /><span>{t}</span><button className="icon-button"><Icon name="edit"/></button></Reveal>)}</div></AdminShell>}
