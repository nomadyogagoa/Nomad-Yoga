import {AdminShell} from "@/components/admin/AdminShell"; import {gallery} from "@/data/extended"; import {Icon} from "@/components/ui/Icon";
export default function GalleryAdmin(){return <AdminShell active="Gallery" title="Media library" subtitle="Upload and organize images used across the website." action={<button className="button button-small"><Icon name="plus"/> Upload media</button>}><div className="media-grid">{gallery.map(([t,img])=><article key={t} style={{backgroundImage:`url(${img})`}}><span>{t}</span><button className="icon-button"><Icon name="edit"/></button></article>)}</div></AdminShell>}
