import Link from "next/link"; import {SiteShell} from "@/components/shared/SiteShell"; import {PageHero} from "@/components/shared/PageHero"; import {plans} from "@/data/extended"; import {Icon} from "@/components/ui/Icon"; import {Reveal} from "@/components/ui/Reveal";
export default function Pricing(){return <SiteShell><PageHero eyebrow="Simple membership" title={<>Practice in a way that <em>fits your life.</em></>} copy="Flexible options for first visits, regular rituals and deeper seasonal practice." image="https://images.unsplash.com/photo-1506629082955-511b1aa562c8?auto=format&fit=crop&w=2000&q=88"/><section className="section"><div className="container"><div className="pricing-grid">{plans.map((p,i)=><Reveal as="article" className={`price-card ${p.popular?'featured':''}`} key={p.name} delay={i*0.1}>{p.popular&&<span className="price-badge">Most loved</span>}<p className="eyebrow">{p.name}</p><div className="price-line"><strong>{p.price}</strong><span>/ {p.period}</span></div><p>{p.description}</p><div className="price-features">{p.features.map(f=><span key={f}><Icon name="check"/> {f}</span>)}</div><Link className={`button ${p.popular?'':'button-outline'}`} href="/register">Choose membership</Link></Reveal>)}</div><Reveal as="div" className="faq-mini" delay={0.2}><div><p className="eyebrow">Need help choosing?</p><h2>Come as you are.</h2></div><p>Start with a drop-in class or book a studio conversation. We’ll help you choose a rhythm that feels sustainable, not overwhelming.</p></Reveal></div></section></SiteShell>}
