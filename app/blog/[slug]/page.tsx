import {notFound} from "next/navigation"; import Link from "next/link"; import {SiteShell} from "@/components/shared/SiteShell"; import {posts} from "@/data/extended";
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <SiteShell><article className="article-page"><div className="container article-head"><Link href="/blog" className="back-link dark">← Journal</Link><p className="eyebrow">{p.tag} · {p.date}</p><h1>{p.title}</h1><p>{p.excerpt}</p></div><div className="article-image" style={{backgroundImage:`url(${p.image})`}}/><div className="container article-body"><p className="lead">Practice changes when we stop treating consistency as perfection.</p><p>There are seasons when movement feels natural and seasons when simply arriving is the practice. Returning with curiosity gives the body and mind room to begin from where they are today.</p><h2>Begin with less</h2><p>Choose one small ritual you can repeat without negotiation: three breaths, ten minutes of mobility, or one weekly class. Sustainable practice grows from trust, not pressure.</p><blockquote>“The quieter the beginning, the easier it is to hear what you actually need.”</blockquote><p>Over time, those small returns become a relationship with yourself — one built on attention, patience and enough space to change.</p></div></article></SiteShell>}
