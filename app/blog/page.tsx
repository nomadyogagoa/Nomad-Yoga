import Link from "next/link"; import {SiteShell} from "@/components/shared/SiteShell"; import {PageHero} from "@/components/shared/PageHero"; import {posts} from "@/data/extended";
export default function Blog(){return <SiteShell><PageHero eyebrow="Journal" title={<>Notes for a more <em>intentional life.</em></>} copy="Practice notes, teacher reflections and simple ideas to carry beyond the mat." image="https://images.unsplash.com/photo-1455390582262-044cdead277a?auto=format&fit=crop&w=2000&q=88"/><section className="section"><div className="container blog-grid">{posts.map(p=><Link href={`/blog/${p.slug}`} className="blog-card" key={p.slug}><div className="blog-image" style={{backgroundImage:`url(${p.image})`}}/><div><p className="eyebrow">{p.tag} · {p.date}</p><h2>{p.title}</h2><p>{p.excerpt}</p><span className="text-link">Read journal →</span></div></Link>)}</div></section></SiteShell>}
