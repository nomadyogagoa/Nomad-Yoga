import { ConflictException, Injectable, NotFoundException, ServiceUnavailableException } from '@nestjs/common'; import { NewsletterSubscriberStatus } from '@prisma/client'; import { pageResult, pagination } from '../../common/pagination/pagination'; import { PrismaService } from '../../database/prisma.service'; import { EmailService } from '../email/email.service';
@Injectable() export class NewsletterService { constructor(private p:PrismaService,private email:EmailService){} async subscribe(email:string,firstName?:string){email=email.trim().toLowerCase();const x=await this.p.newsletterSubscriber.findUnique({where:{email}});if(x){if(x.status!==NewsletterSubscriberStatus.ACTIVE)await this.p.newsletterSubscriber.update({where:{id:x.id},data:{status:NewsletterSubscriberStatus.ACTIVE,unsubscribedAt:null,firstName:firstName??x.firstName}});return{accepted:true}}await this.p.newsletterSubscriber.create({data:{email,firstName,status:NewsletterSubscriberStatus.ACTIVE}});return{accepted:true}} async unsubscribe(email:string){const x=await this.p.newsletterSubscriber.findUnique({where:{email:email.trim().toLowerCase()}});if(x)await this.p.newsletterSubscriber.update({where:{id:x.id},data:{status:NewsletterSubscriberStatus.UNSUBSCRIBED,unsubscribedAt:new Date()}});return{accepted:true}} async subscribers(q:any){const where:any={...(q.status?{status:q.status}:{}),...(q.search?{email:{contains:q.search,mode:'insensitive'}}:{})};const[items,total]=await Promise.all([this.p.newsletterSubscriber.findMany({where,...pagination(q)}),this.p.newsletterSubscriber.count({where})]);return pageResult(items,total,q)} async campaign(id:string){const x=await this.p.newsletterCampaign.findUnique({where:{id},include:{deliveries:true}});if(!x)throw new NotFoundException({code:'NEWSLETTER_CAMPAIGN_NOT_FOUND',message:'Campaign not found.'});return x} campaigns(q:any){return this.p.newsletterCampaign.findMany({...pagination(q),orderBy:{createdAt:'desc'}})} createCampaign(d:any){return this.p.newsletterCampaign.create({data:d})} updateCampaign(id:string,d:any){return this.p.newsletterCampaign.update({where:{id},data:d})} async prepare(id:string){await this.campaign(id);const subs=await this.p.newsletterSubscriber.findMany({where:{status:NewsletterSubscriberStatus.ACTIVE}});for(const s of subs)await this.p.newsletterDelivery.upsert({where:{campaignId_subscriberId:{campaignId:id,subscriberId:s.id}},create:{campaignId:id,subscriberId:s.id},update:{}});return{prepared:subs.length,sent:false}} async sendCampaign(id:string){const campaign=await this.campaign(id);if(campaign.status==='SENT')throw new ConflictException({code:'NEWSLETTER_CAMPAIGN_ALREADY_SENT',message:'Campaign has already been sent.'});const subscribers=await this.p.newsletterSubscriber.findMany({where:{status:NewsletterSubscriberStatus.ACTIVE},select:{id:true,email:true}});if(!subscribers.length)throw new NotFoundException({code:'NEWSLETTER_NO_ELIGIBLE_RECIPIENTS',message:'No eligible newsletter recipients.'});let sent=0,failed=0;for(let start=0;start<subscribers.length;start+=25){for(const sub of subscribers.slice(start,start+25)){const delivery=await this.p.newsletterDelivery.upsert({where:{campaignId_subscriberId:{campaignId:id,subscriberId:sub.id}},create:{campaignId:id,subscriberId:sub.id},update:{}});if(delivery.deliveredAt)continue;try{await this.email.sendNewsletterEmail(sub.email,campaign.subject,campaign.content);await this.p.newsletterDelivery.update({where:{id:delivery.id},data:{deliveredAt:new Date(),failedAt:null,errorMessage:null}});sent++;}catch{await this.p.newsletterDelivery.update({where:{id:delivery.id},data:{failedAt:new Date(),errorMessage:'EMAIL_DELIVERY_FAILED'}});failed++;}}}if(!sent&&failed)throw new ServiceUnavailableException({code:'NEWSLETTER_DELIVERY_FAILED',message:'Newsletter delivery failed.'});await this.p.newsletterCampaign.update({where:{id},data:{status:failed?'PARTIALLY_SENT':'SENT',sentAt:new Date()}});return{sent,failed};} }
