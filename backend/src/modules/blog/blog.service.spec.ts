import { BlogStatus } from '@prisma/client'; import { BlogService } from './blog.service'; describe('BlogService',()=>{it('limits public queries to published posts',async()=>{const p:any={blogPost:{findMany:jest.fn().mockResolvedValue([]),count:jest.fn().mockResolvedValue(0)}};await new BlogService(p,{record:jest.fn()}as any).posts({page:1,limit:20,sortOrder:'desc'}as any);expect(p.blogPost.findMany).toHaveBeenCalledWith(expect.objectContaining({where:expect.objectContaining({status:BlogStatus.PUBLISHED})}))})});
